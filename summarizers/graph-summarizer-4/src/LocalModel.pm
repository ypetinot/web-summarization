package LocalModel;

# Local models allow decoders to perform incremental updates to structure scores (is this is a proper description ?)

use strict;
use warnings;

use Moose::Role;

with('Model');

1;
